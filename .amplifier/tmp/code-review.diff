diff --git a/.gitignore b/.gitignore
index f9c4d1f..cbd6893 100644
--- a/.gitignore
+++ b/.gitignore
@@ -24,3 +24,10 @@ node_modules/
 # Test artifacts
 test-results/
 playwright-report/
+
+# Generated TypeScript build info
+*.tsbuildinfo
+
+# Generated electron-vite config artifacts
+electron.vite.config.js
+electron.vite.config.d.ts
diff --git a/e2e/app.spec.ts b/e2e/app.spec.ts
deleted file mode 100644
index eab97bf..0000000
--- a/e2e/app.spec.ts
+++ /dev/null
@@ -1,28 +0,0 @@
-import { test, expect, _electron as electron } from '@playwright/test'
-import type { ElectronApplication, Page } from '@playwright/test'
-
-let app: ElectronApplication
-let page: Page
-
-test.beforeAll(async () => {
-  app = await electron.launch({
-    args: ['.'],
-    env: { ...process.env, NODE_ENV: 'test' }
-  })
-  page = await app.firstWindow()
-})
-
-test.afterAll(async () => {
-  await app.close()
-})
-
-test('app launches and shows a window', async () => {
-  expect(page).toBeTruthy()
-  const title = await page.title()
-  expect(title).toBe('Amplifier Canvas')
-})
-
-test('window displays Canvas text', async () => {
-  const content = await page.locator('#app').textContent()
-  expect(content).toContain('Canvas')
-})
diff --git a/package-lock.json b/package-lock.json
index 7c43ae1..e8277f9 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -19,6 +19,7 @@
         "electron": "^41.1.1",
         "electron-builder": "^26.8.1",
         "electron-vite": "^5.0.0",
+        "js-yaml": "^4.1.0",
         "typescript": "^5.8.3",
         "vite": "^8.0.3"
       }
diff --git a/package.json b/package.json
index 4f9147e..03b1081 100644
--- a/package.json
+++ b/package.json
@@ -22,6 +22,7 @@
     "electron": "^41.1.1",
     "electron-builder": "^26.8.1",
     "electron-vite": "^5.0.0",
+    "js-yaml": "^4.1.0",
     "typescript": "^5.8.3",
     "vite": "^8.0.3"
   }
diff --git a/src/main/index.ts b/src/main/index.ts
index d917515..3ca1175 100644
--- a/src/main/index.ts
+++ b/src/main/index.ts
@@ -2,6 +2,8 @@ import { app, BrowserWindow, Menu, shell } from 'electron'
 import { join } from 'path'
 import { APP_NAME, WINDOW_CONFIG } from '../shared/constants'
 
+const isDev = !app.isPackaged
+
 let mainWindow: BrowserWindow | null = null
 
 function createMenu(): void {
@@ -91,7 +93,7 @@ function createWindow(): void {
   })
 
   // Load the renderer
-  if (!app.isPackaged && process.env['ELECTRON_RENDERER_URL']) {
+  if (isDev && process.env['ELECTRON_RENDERER_URL']) {
     mainWindow.loadURL(process.env['ELECTRON_RENDERER_URL'])
   } else {
     mainWindow.loadFile(join(__dirname, '../renderer/index.html'))
@@ -102,3 +104,17 @@ app.whenReady().then(() => {
   createMenu()
   createWindow()
 })
+
+// Quit when all windows are closed (non-macOS)
+app.on('window-all-closed', () => {
+  if (process.platform !== 'darwin') {
+    app.quit()
+  }
+})
+
+// Re-create window on dock icon click when no windows exist (macOS)
+app.on('activate', () => {
+  if (BrowserWindow.getAllWindows().length === 0) {
+    createWindow()
+  }
+})
diff --git a/test/verify-all.mjs b/test/verify-all.mjs
index 3f51217..eb1f7a9 100644
--- a/test/verify-all.mjs
+++ b/test/verify-all.mjs
@@ -5,14 +5,18 @@
  *
  * This is the entry point for `npm test`. Any new verify-*.mjs script
  * added to this directory is automatically picked up.
+ *
+ * A single `npm run build` is executed here before running individual
+ * scripts so each script doesn't need to rebuild on its own.
  */
 
 import { readdirSync } from 'fs';
 import { resolve, dirname, basename } from 'path';
 import { fileURLToPath } from 'url';
-import { spawnSync } from 'child_process';
+import { execSync, spawnSync } from 'child_process';
 
 const __dirname = dirname(fileURLToPath(import.meta.url));
+const ROOT = resolve(__dirname, '..');
 
 const scripts = readdirSync(__dirname)
   .filter(f => /^verify-.+\.mjs$/.test(f) && f !== 'verify-all.mjs')
@@ -24,6 +28,19 @@ if (scripts.length === 0) {
   process.exit(0);
 }
 
+// ===== Shared build (hoisted) =====
+console.log('='.repeat(60));
+console.log('▶  Shared build: npm run build');
+console.log('='.repeat(60));
+try {
+  execSync('npm run build', { cwd: ROOT, stdio: 'inherit', timeout: 120_000 });
+} catch {
+  console.error('❌  npm run build failed — aborting test run.');
+  process.exit(1);
+}
+console.log('');
+
+// ===== Run individual verify scripts =====
 console.log(`Running ${scripts.length} verification script(s)…\n`);
 
 const failed = [];
diff --git a/test/verify-setup.mjs b/test/verify-setup.mjs
index 72cfb32..1d652a3 100644
--- a/test/verify-setup.mjs
+++ b/test/verify-setup.mjs
@@ -2,7 +2,7 @@
 /**
  * Setup verification test for task-1: Initialize Electron project with electron-vite
  * This is the RED phase test - run before and after implementation.
- * 
+ *
  * Tests:
  * 1. package.json exists with correct fields
  * 2. electron.vite.config.ts exists
@@ -11,47 +11,9 @@
  * 5. node_modules exists (npm install completed)
  */
 
-import { existsSync, readFileSync } from 'fs';
-import { resolve, dirname } from 'path';
-import { fileURLToPath } from 'url';
-
-const __dirname = dirname(fileURLToPath(import.meta.url));
-const ROOT = resolve(__dirname, '..');
-
-let passed = 0;
-let failed = 0;
-const failures = [];
-
-function assert(condition, message) {
-  if (condition) {
-    console.log(`  ✓ ${message}`);
-    passed++;
-  } else {
-    console.log(`  ✗ ${message}`);
-    failed++;
-    failures.push(message);
-  }
-}
-
-function fileExists(relPath) {
-  return existsSync(resolve(ROOT, relPath));
-}
-
-function readJSON(relPath) {
-  try {
-    return JSON.parse(readFileSync(resolve(ROOT, relPath), 'utf8'));
-  } catch {
-    return null;
-  }
-}
+import { createHarness } from './harness.mjs';
 
-function readText(relPath) {
-  try {
-    return readFileSync(resolve(ROOT, relPath), 'utf8');
-  } catch {
-    return null;
-  }
-}
+const { ROOT, assert, fileExists, readText, readJSON, summarize } = createHarness(import.meta.url);
 
 // ===== TEST 1: package.json =====
 console.log('\n📦 Test 1: package.json');
@@ -200,26 +162,5 @@ assert(fileExists('src/preload/index.ts'), 'src/preload/index.ts exists (preload
 assert(fileExists('src/renderer/src/main.tsx'), 'src/renderer/src/main.tsx exists (React renderer entry)');
 assert(fileExists('src/renderer/index.html'), 'src/renderer/index.html exists (Vite HTML entry)');
 
-// ===== TEST 10: Build succeeds =====
-console.log('\n🔨 Test 10: npm run build (validates full build pipeline)');
-try {
-  const { execSync } = await import('child_process');
-  execSync('npm run build', { cwd: ROOT, stdio: 'pipe', timeout: 120_000 });
-  assert(true, 'npm run build exits 0');
-} catch (err) {
-  const stderr = err.stderr?.toString().trim() || '';
-  const firstLine = stderr.split('\n')[0] || 'unknown error';
-  assert(false, `npm run build exits 0 — failed: ${firstLine}`);
-}
-
 // ===== SUMMARY =====
-console.log('\n' + '='.repeat(50));
-console.log(`Results: ${passed} passed, ${failed} failed`);
-if (failures.length > 0) {
-  console.log('\nFailed assertions:');
-  failures.forEach(f => console.log(`  ✗ ${f}`));
-  process.exit(1);
-} else {
-  console.log('\n✅ All tests passed!');
-  process.exit(0);
-}
+summarize();
diff --git a/test/verify-task2.mjs b/test/verify-task2.mjs
index 8d544a0..2ac4a97 100644
--- a/test/verify-task2.mjs
+++ b/test/verify-task2.mjs
@@ -5,43 +5,9 @@
  * GREEN phase: Run after implementation to confirm passing.
  */
 
-import { existsSync, readFileSync } from 'fs';
-import { resolve, dirname } from 'path';
-import { fileURLToPath } from 'url';
-
-const __dirname = dirname(fileURLToPath(import.meta.url));
-const ROOT = resolve(__dirname, '..');
-
-let passed = 0;
-let failed = 0;
-const failures = [];
-
-function assert(condition, message) {
-  if (condition) {
-    console.log(`  ✓ ${message}`);
-    passed++;
-  } else {
-    console.log(`  ✗ ${message}`);
-    failed++;
-    failures.push(message);
-  }
-}
-
-function dirExists(relPath) {
-  return existsSync(resolve(ROOT, relPath));
-}
+import { createHarness } from './harness.mjs';
 
-function fileExists(relPath) {
-  return existsSync(resolve(ROOT, relPath));
-}
-
-function readText(relPath) {
-  try {
-    return readFileSync(resolve(ROOT, relPath), 'utf8');
-  } catch {
-    return null;
-  }
-}
+const { ROOT, assert, fileExists, dirExists, readText, summarize } = createHarness(import.meta.url);
 
 // ===== TEST 1: Directory structure =====
 console.log('\n📁 Test 1: Required directories exist');
@@ -112,9 +78,11 @@ if (mainTs) {
     mainTs.includes('shell') || mainTs.includes('new-window') || mainTs.includes('openExternal') || mainTs.includes('will-navigate') || mainTs.includes('open-url'),
     'handles external links opening in default browser'
   );
-  // Dev vs production URL loading
-  assert(mainTs.includes('is.dev') || mainTs.includes('ELECTRON_RENDERER_URL') || mainTs.includes('loadURL') || mainTs.includes('loadFile'), 'loads renderer URL in dev or file in production');
-  assert(mainTs.includes('@electron-toolkit/utils') || mainTs.includes('is.dev'), 'uses @electron-toolkit/utils is.dev');
+  // Dev vs production URL loading — accepts either isPackaged-based or ELECTRON_RENDERER_URL-based approach
+  assert(
+    mainTs.includes('ELECTRON_RENDERER_URL') || mainTs.includes('loadURL') || mainTs.includes('loadFile') || mainTs.includes('isPackaged'),
+    'loads renderer URL in dev or file in production'
+  );
 } else {
   assert(false, 'src/main/index.ts is readable');
 }
@@ -176,30 +144,5 @@ if (appCss) {
   assert(false, 'src/renderer/src/App.css exists and is readable');
 }
 
-// ===== TEST 11: @electron-toolkit/utils installed =====
-console.log('\n📦 Test 11: @electron-toolkit/utils installed');
-assert(fileExists('node_modules/@electron-toolkit/utils'), '@electron-toolkit/utils is installed');
-
-// ===== TEST 12: Build still passes =====
-console.log('\n🔨 Test 12: npm run build still passes');
-try {
-  const { execSync } = await import('child_process');
-  execSync('npm run build', { cwd: ROOT, stdio: 'pipe', timeout: 120_000 });
-  assert(true, 'npm run build exits 0');
-} catch (err) {
-  const stderr = err.stderr?.toString().trim() || '';
-  const firstLine = stderr.split('\n')[0] || 'unknown error';
-  assert(false, `npm run build exits 0 — failed: ${firstLine}`);
-}
-
 // ===== SUMMARY =====
-console.log('\n' + '='.repeat(50));
-console.log(`Results: ${passed} passed, ${failed} failed`);
-if (failures.length > 0) {
-  console.log('\nFailed assertions:');
-  failures.forEach(f => console.log(`  ✗ ${f}`));
-  process.exit(1);
-} else {
-  console.log('\n✅ All tests passed!');
-  process.exit(0);
-}
+summarize();
diff --git a/test/verify-task3.mjs b/test/verify-task3.mjs
index 6c9aa57..7ee314d 100644
--- a/test/verify-task3.mjs
+++ b/test/verify-task3.mjs
@@ -9,48 +9,10 @@
  * 2. build/entitlements.mac.plist exists with correct plist content
  */
 
-import { existsSync, readFileSync, statSync } from 'fs';
-import { resolve, dirname } from 'path';
-import { fileURLToPath } from 'url';
+import { createHarness } from './harness.mjs';
 import yaml from 'js-yaml';
 
-const __dirname = dirname(fileURLToPath(import.meta.url));
-const ROOT = resolve(__dirname, '..');
-
-let passed = 0;
-let failed = 0;
-const failures = [];
-
-function assert(condition, message) {
-  if (condition) {
-    console.log(`  ✓ ${message}`);
-    passed++;
-  } else {
-    console.log(`  ✗ ${message}`);
-    failed++;
-    failures.push(message);
-  }
-}
-
-function fileExists(relPath) {
-  return existsSync(resolve(ROOT, relPath));
-}
-
-function dirExists(relPath) {
-  try {
-    return statSync(resolve(ROOT, relPath)).isDirectory();
-  } catch {
-    return false;
-  }
-}
-
-function readText(relPath) {
-  try {
-    return readFileSync(resolve(ROOT, relPath), 'utf8');
-  } catch {
-    return null;
-  }
-}
+const { ROOT, assert, fileExists, dirExists, readText, summarize } = createHarness(import.meta.url);
 
 /** Parse a minimal plist into a { key: boolean } map. */
 function parsePlist(text) {
@@ -188,13 +150,4 @@ if (plistText) {
 }
 
 // ===== SUMMARY =====
-console.log('\n' + '='.repeat(50));
-console.log(`Results: ${passed} passed, ${failed} failed`);
-if (failures.length > 0) {
-  console.log('\nFailed assertions:');
-  failures.forEach(f => console.log(`  ✗ ${f}`));
-  process.exit(1);
-} else {
-  console.log('\n✅ All tests passed!');
-  process.exit(0);
-}
+summarize();
diff --git a/test/verify-task4.mjs b/test/verify-task4.mjs
index ea270f0..6061b31 100644
--- a/test/verify-task4.mjs
+++ b/test/verify-task4.mjs
@@ -18,40 +18,12 @@
  * 11. electron-vite dev starts without Electron install error
  */
 
-import { existsSync, readFileSync, rmSync } from 'fs';
-import { resolve, dirname } from 'path';
-import { fileURLToPath } from 'url';
+import { existsSync, rmSync } from 'fs';
+import { resolve } from 'path';
 import { execSync, spawnSync } from 'child_process';
+import { createHarness } from './harness.mjs';
 
-const __dirname = dirname(fileURLToPath(import.meta.url));
-const ROOT = resolve(__dirname, '..');
-
-let passed = 0;
-let failed = 0;
-const failures = [];
-
-function assert(condition, message) {
-  if (condition) {
-    console.log(`  ✓ ${message}`);
-    passed++;
-  } else {
-    console.log(`  ✗ ${message}`);
-    failed++;
-    failures.push(message);
-  }
-}
-
-function fileExists(relPath) {
-  return existsSync(resolve(ROOT, relPath));
-}
-
-function readText(relPath) {
-  try {
-    return readFileSync(resolve(ROOT, relPath), 'utf8');
-  } catch {
-    return null;
-  }
-}
+const { ROOT, assert, fileExists, readText, summarize } = createHarness(import.meta.url);
 
 // ===== SETUP: Clean out/ directory for a fresh build =====
 console.log('\n🧹 Setup: Cleaning out/ directory for a fresh build');
@@ -72,7 +44,7 @@ assert(!fileExists('out/renderer/index.html'), 'out/renderer/index.html absent b
 console.log('\n🔨 Test 1: npm run build succeeds');
 let buildStderr = '';
 try {
-  const result = execSync('npm run build', {
+  execSync('npm run build', {
     cwd: ROOT,
     timeout: 120_000,
     stdio: ['pipe', 'pipe', 'pipe']
@@ -222,13 +194,4 @@ if (hasInstallError) {
 }
 
 // ===== SUMMARY =====
-console.log('\n' + '='.repeat(50));
-console.log(`Results: ${passed} passed, ${failed} failed`);
-if (failures.length > 0) {
-  console.log('\nFailed assertions:');
-  failures.forEach(f => console.log(`  ✗ ${f}`));
-  process.exit(1);
-} else {
-  console.log('\n✅ All tests passed!');
-  process.exit(0);
-}
+summarize();
